apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.component }}
  labels:
    appName: {{ .Values.labels.appName }}
    project_Name: {{ .Values.labels.appName }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.component }}
                port:
                  number: 80
